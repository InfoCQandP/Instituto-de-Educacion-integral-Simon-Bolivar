function mostrarMensaje() {
    alert("Mensaje enviado correctamente");

      // Limpiar los campos de entrada de texto
      location.reload();
  }
  
  function mostrarMensaje(preescolar) {
    alert("Sólo es un ejemplo " + preescolar);
    location.reload();
  }

  function mostrarMensaje(primaria) {
    alert("Sólo es un ejemplo " + primaria);
    location.reload();
  }

  function mostrarMensaje(secundaria) {
    alert("Sólo es un ejemplo " + secundaria);
    location.reload();
  }

  function mostrarMensaje(preparatoria) {
    alert("Sólo es un ejemplo " + preparatoria);
    location.reload();
  }